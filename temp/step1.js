const fs = require("fs"); const path = require("path"); const filePath = path.join(__dirname, "components/creation/generated-image-gallery.tsx"); const tempFile = path.join(__dirname, "temp/gallery-optimizer.js"); fs.writeFileSync(tempFile, `const fs = require("fs"); const path = require("path"); const filePath = path.join(__dirname, "../components/creation/generated-image-gallery.tsx"); const content = fs.readFileSync(filePath, "utf8"); console.log("已读取文件，长度:", content.length); console.log("请继续执行下一个脚本");`); console.log("初始脚本已创建");
